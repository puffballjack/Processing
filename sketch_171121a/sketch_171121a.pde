PVector pos;
PVector vel;
int radius;

void setup() {
  background(0);
  size(600, 600);
  radius = 10;
  pos = new PVector(width/2, height/2);
  vel = new PVector(0, 2);
}

void draw() {
  background(0);
  fill(255);
  ellipse(
}